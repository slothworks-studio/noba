import { DeckStore } from './deckStore';

describe('initialize', () => {
  test('it loads the deck from the file system', () => {
    DeckStore.
  });
  test('it shuffles the deck', () => {});
  test('it loads the state from local storage', () => {});
  test('it persists the state to local storage', () => {});
  test('it checks to see if there is state saved to local storage', () => {});
});

describe('functionalilty', () => {
  test('it returns the text of the next card', () => {});
  test('it marks a card as completed', () => {});
});
